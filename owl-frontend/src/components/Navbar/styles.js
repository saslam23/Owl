import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) =>({
    root: {
        flexGrow: 1,
        display: 'flex',
        height: 224,
        margin:'20rem 0 0 10rem'
      },
      tabs: {
        color:'white',
        fontSize:'1.2rem',
        marginLeft:'2rem'
      },
}))